# 2026 A题《药材的烘干问题》配图方案

依据：用户提供的 A题.zip 内题面4页、附件1、附件2以及4个结果模板；并参考本次已阅读的18篇往年A题论文。本文是待模型求解后落实的配图设计，没有把示意或预期趋势当作计算结果。

**推荐正文安排8个主图编号，另设少量按需补图。** 用户原草案覆盖了主要任务；调整重点是合并问题3的两张图、补足问题4的达标判据，并让输入、模型、结果和检验各自承担清楚的作用。

\[
\underbrace{\text{环境输入与物理结构}}_{\text{图1—2}}
\longrightarrow
\underbrace{\text{预热分布与短时演化}}_{\text{图3—4}}
\longrightarrow
\underbrace{\text{全程达标与收缩情形}}_{\text{图5—7}}
\longrightarrow
\underbrace{\text{数值精度检验}}_{\text{图8}}.
\]

## 1. 先核对题面，确定图的边界

| 事项 | 题面或附件事实 | 对配图的影响 |
|---|---|---|
| 几何与初值 | 圆柱长25 cm、初始半径2 cm，初温28°C，初始干基含水率2.55 kg/kg | 原理图写真实尺寸；一维径向模型须说明忽略轴向/端部差异的近似 |
| 问题1 | 规定100、300、600、900、1200、1500、1800 s，以及0、0.5、1、1.5、2 cm的表格；完整结果每1 s、0.1 cm输出 | 径向曲线对应这些时刻；图不代替表1、表2及result1.xlsx |
| 问题2 | 模型覆盖整个烘干过程，正文重点给出前3 h每0.5 h的结果；统一使用附录3经验式 | 前3 h画时空图；不要把问题1结果与问题2结果直接拼接 |
| 问题3 | 药材各处含水率严格低于0.15 kg/kg；结果按6 h列表，完整结果按60 s、0.1 cm输出 | 最大含水率曲线是核心；6 h是报告间隔，不能当作终点求解精度 |
| 问题4 | 半径取自附件2，经验公式改为附录4；结果表另列药材表面 | 图上画实际移动表面；半径外留白，数值和表面值在实际位置取样 |
| 环境输入 | 附件1为0—14400 s，即0—4 h；末段在约50°C、0.05 kg/kg附近波动 | 原始点+实际采用的输入函数；超过4 h的恒定延拓等属于模型假设 |
| 收缩输入 | 附件2为0—259200 s，即0—72 h；半径由2降到1.198 cm | 画完整观测范围和早期放大；1.2 cm只能说近似平台，不能称严格不变的极限 |

**1800 s不是题面给定的预热/恒温切换时刻。** 附件1在1800 s的环境温度为41.513°C、环境水分浓度为0.03307 kg/kg。图上可以标“问题1展示范围”，但阶段分区应依据最终模型的明确判据。题面还要求问题2、3统一采用附录3，不能仅因时间到1800 s就从附录2切换参数。

温度图可用°C；扩散系数经验公式中的温度须按题面使用K。药材水分浓度是干基含水率，坐标写 \(C/(\mathrm{kg\,kg^{-1}})\)，不能标成相对湿度。环境量用 \(T_\infty,C_\infty\)，内部量用 \(T,C\)，图例明确区分。

## 2. 正文主图总表

| 图号与位置 | 推荐图题 | 子图与坐标 | 主要读图任务 |
|---|---|---|---|
| 图1：数据与边界条件 | 烘房环境变化及边界输入重建 | 上：时间h—环境温度°C；下：时间h—环境水分浓度kg/kg；共享0—4 h时间轴 | 说明边界输入来源、数据波动与采用的输入函数 |
| 图2：模型建立 | 圆柱药材径向传热传质及边界条件示意 | 圆柱整体＋横截面/径向放大；标长、半径、中心、环境量与热质交换 | 说明几何近似、径向坐标及边界含义 |
| 图3：问题1结果 | 预热阶段代表时刻的径向温度与含水率分布 | 左：半径cm—温度°C；右：半径cm—含水率kg/kg | 说明同一时刻中心与表面的差异，以及径向梯度随时间如何变化 |
| 图4：问题2结果 | 前3小时温度与含水率的时空演化 | 上排：时间h—半径cm的温度/含水率热图；下排：相应中心、中间、表面时间曲线 | 从场分布定位变化，再由曲线读出变化速度和滞后 |
| 图5：问题3结果 | 固定半径模型的全域干燥判据与空间进程 | 上：时间h—最大含水率，带阈值和交点放大；下：时间h—半径cm的含水率热图与0.15等值线 | 直接给出终止时刻，并解释局部区域如何接近达标 |
| 图6：问题4建模 | 药材收缩规律与移动区域表示 | 左：时间h—半径cm，观测点＋采用函数；右：物理区域与归一化坐标示意（采用此计算法时） | 交代半径函数来源，以及移动边界如何进入计算 |
| 图7：问题4结果 | 收缩条件下的含水率演化与干燥时间 | 上：时间h—实际半径cm的含水率热图，画移动表面；下：问题3、4的全域最大含水率曲线，带阈值与终点 | 显示实际有效区域，并直接支持问题4烘干时长 |
| 图8：数值验证 | 空间与时间离散对干燥终点的影响 | 左：空间步长—终点偏差；右：时间步长—终点偏差；分别固定另一个离散精度 | 检查最终报告时长是否对计算精度足够稳定 |

图号表示一幅可含多个子图的组合图，不是8个单独坐标轴。图4若2×2排版后字号不足，保留上排热图，下面时间曲线移至附录或改为关键位置的小窗；不要压缩到不可读。

## 3. 每幅图的具体画法

### 图1：环境输入——保留原始点，也画真正送入模型的函数

- 时间轴为0—4 h；两幅子图纵向排列，温度和水分浓度各有自己的纵轴。
- 原始数据用小型浅灰散点，模型使用的输入函数用清晰实线。点较密时缩小标记，不静默删除观测。
- 在0—0.5 h区域用极浅背景或细括号标“问题1结果范围”，避免写成未经确定的“预热阶段终点”。
- 输入函数可根据模型选择分段线性、保形分段插值或有依据的拟合。图例必须写实际采用的办法，不把“插值”和“拟合”混用；插值不自动具有去噪作用。
- 正文交代4 h之后的边界设定；若图上展示延拓，使用虚线并明确标“模型延拓”，不能画成观测范围的一部分。没必要为了展示两三天延拓而把前4 h挤成一小段，可另设小窗。
- 图的作用是说明输入重建；输入点与线吻合不等于药材内部场已被实验验证。

### 图2：物理结构——整体与径向局部配对

左侧用简洁圆柱显示 \(L=25\,\mathrm{cm}\)、初始 \(R_0=2\,\mathrm{cm}\)；右侧用横截面和一条径向计算线显示 \(r=0\)、\(r=R\)、径向网格或控制体。

中心处注明对称条件：
\[
\left.\frac{\partial T}{\partial r}\right|_{r=0}=0,\qquad
\left.\frac{\partial C}{\partial r}\right|_{r=0}=0.
\]

表面标外界 \(T_\infty(t),C_\infty(t)\)，用橙色箭头表示热交换、蓝色箭头表示水分迁移，具体边界公式以最终采用的模型为准。干燥示意可画热流进入、水分向外，但箭头说明的是示意中的工况，不代替所有时刻的数值通量判断。

图下注明“一维径向近似，忽略轴向及端部差异”。几何图可以不严格按长径比例排版，但需要标“示意图”；不要让插画细节遮住坐标和边界。

借鉴 A016/A196 的“整体对象＋局部解释”，本题不需要另加没有解释作用的三维实体渲染。

### 图3：问题1——径向剖面

\[
T(r,t_i),\quad C(r,t_i),\qquad
t_i\in\{100,300,600,900,1200,1500,1800\}\,\mathrm{s}.
\]

两图横轴均为 \(r\in[0,2]\,\mathrm{cm}\)，主刻度0、0.5、1、1.5、2；内部求解和输出仍可比这些刻度更密。

- 初稿保留7个规定时刻，同一时刻在左右图中使用同一颜色和线型，共用一个图例。
- 若曲线过密，正文保留100、600、1200、1800 s四个代表时刻，完整7个时刻仍在规定表格和结果文件中给出。
- 曲线采用随时间逐渐加深的顺序色；最浅曲线也应在白底上可见。
- 不给每个输出网格点都加标记；只在少数关键位置用标记辅助辨认。
- 读图重点是中心—表面差异和剖面形态。温度或浓度差很小时，可加差值小窗：
\[
\Delta T(t)=T(R,t)-T(0,t),\qquad
\Delta C(t)=C(0,t)-C(R,t).
\]
差值图必须明确正负定义，不能将局部自动缩放造成的视觉幅度误当成实际巨大差异。

图后结论依据实际曲线填写，不预先断言所有剖面均单调或中心必为最大值。

### 图4：问题2——场图定位，时间曲线读数

推荐2×2布局：

| | 温度 | 含水率 |
|---|---|---|
| 上排 | \(T(r,t)\) 热力图 | \(C(r,t)\) 热力图 |
| 下排 | \(r=0,1,2\,\mathrm{cm}\) 的温度曲线 | 同一位置的含水率曲线 |

时间范围统一为0—3 h，热图半径范围统一为0—2 cm；横轴主刻度可用0.5 h，与表3、表4相对应。下排同一径向位置颜色和线型一致，位置图例可共用；需要说明边界响应时，温度曲线增加灰色虚线环境温度。

温度与含水率分别用独立色条和单位。温度可用连续暖色，含水率可用连续蓝色；不得共用一个混合量纲色条。

如果温度很快接近均匀，保留真实色标，利用中心—表面差值或早期放大说明小尺度差异；不要按每个时刻分别重新归一化热图颜色。若底部三条曲线长期重合，可精简为中心/表面两条或差值曲线。

### 图5：问题3——终点判据与空间过程合为一幅

对固定半径定义
\[
M_3(t)=\max_{0\le r\le R_0}C_3(r,t).
\]

上图用深蓝线画 \(M_3(t)\)，红色虚线画 \(C_{\mathrm{crit}}=0.15\,\mathrm{kg/kg}\)，标临界附近时刻并设置交点放大。下图画全程含水率场，叠加 \(C=0.15\) 等值线；上下共享时间轴，以便读者把局部临界位置与全域判据联系起来。

连续解下可定义
\[
t_q^{\mathrm{crit}}
=\inf\{t\ge0:M_q(t)<C_{\mathrm{crit}}\},\qquad q=3,4.
\]

在连续向下穿越的情形，临界边界处可能正好等于0.15；题面要求的是严格低于。数值实现应保留“尚未满足—已满足”的时间夹逼：
\[
M_q(t_-)\ge C_{\mathrm{crit}},\qquad
M_q(t_+)<C_{\mathrm{crit}},
\]
并在局部细化 \(t_+-t_-\)。图中可标临界估计值，报告中另说明严格条件的确认和终点精度。使用未四舍五入结果判定，不能依靠表里显示的0.1500决定是否达标；若出现多次交叉，应检查终点定义和后续是否反弹。

其他细节：

- 最大值从整个材料域计算；没有验证最大值位置前，不以中心值或平均值替代。
- 保存最大值位置，检查表面和内部网格；需要时在热图标出最后接近临界的位置。有限网格上的最大值仍需结合空间加密检验。
- 全程热图避免一味追求低浓度区域的颜色反差。全域最大值曲线与阈值等值线负责精确判读；若另给末段色标放大，必须标注其范围。
- 若阈值等值线没有提供额外空间解释，可将下图移到附录，正文保留判据曲线。
- 表5与result3.xlsx从同一份高精度结果生成，最后才按要求保留四位小数。

### 图6：问题4——半径函数与计算区域

左图使用附件2的全部0—72 h数据：灰色散点表示观测，绿色实线表示真正进入模型的 \(R(t)\)。可加0—6 h局部放大，展示初期收缩速度变化。图注写清插值/拟合方法；若使用平滑拟合，可按需增加残差小窗，逐点插值的零残差没有必要单独展示。

若采用归一化坐标，在右图说明
\[
\xi=\frac{r}{R(t)},\qquad 0\le\xi\le1,
\]
将物理时间—半径平面中的收缩区域对应到时间—\(\xi\)平面中的矩形区域。横轴都为时间，左侧纵轴单位cm，右侧纵轴无量纲；以箭头显示同一时刻的坐标映射。

固定 \(\xi\) 表示固定相对径向位置；在未给出材料运动学假设前，不称其为固定物质点。图示也不意味着只需改坐标而不用处理相应的移动区域方程。

如果实际采用直接移动网格或其他办法，右图改为实际网格方法；不要为了图好看而画一个没有用于求解的算法。若不需要解释映射，这幅图可只有半径曲线，并在图2补一个收缩轮廓。

### 图7：问题4——实际区域与终点都要看得见

上图：
\[
C_4(r,t),\qquad 0\le r\le R(t).
\]

横轴时间h，纵轴实际半径cm，画粗实线 \(r=R(t)\)，叠加 \(C=0.15\) 等值线。半径外区域留白或浅灰并标“材料域外”；它不是零含水率，不能纳入色标、最大值、平均值或质量积分。

为便于与问题3比较，默认纵轴也固定0—2 cm，含水率使用一致的颜色含义和色标范围。若两图时长不同，各自终点明确标注；直接场图比较应使用一致的共同时间窗口，不能把0—100%的归一化进程冒充相同物理时刻。

下图画
\[
M_4(t)=\max_{0\le r\le R(t)}C_4(r,t)
\]
及 \(M_3(t)\)，叠加0.15阈值，标 \(t_3,t_4\) 并放大临界附近。两种模型在相同物理时间轴上对照，图例写全：

- 问题3：附录3物性、固定半径；
- 问题4：附录4物性、变化半径。

**这个对照显示两种题设模型条件下的综合差异。** 若希望进一步声称“收缩造成了多少时长变化”，需补一组使用附录4物性但保持 \(R=R_0\) 的控制计算：
\[
\Delta t_{\mathrm{shrink}\mid\mathrm{A4}}
=t^\star(\text{附录4},R(t))
-t^\star(\text{附录4},R_0).
\]
这是在附录4物性条件下、其他设定保持一致时的几何作用对比，不等同于把问题3到问题4的全部差异都分解完毕。未做控制计算时，不使用“收缩使干燥缩短×%”的图题。

表6及result4.xlsx仍按实际径向位置输出，并单列当时表面值；不将无量纲坐标样本直接填进标注cm的表格。不存在的外域位置与真实表面必须区分。

### 图8：数值验证——检查最终报告量

分别进行空间和时间加密，至少三档，例如：

\[
E_r(h)=|t^\star(h,\Delta t_{\mathrm{fine}})
-t^\star(h_{\mathrm{ref}},\Delta t_{\mathrm{fine}})|,
\]
\[
E_t(\Delta t)=|t^\star(h_{\mathrm{fine}},\Delta t)
-t^\star(h_{\mathrm{fine}},\Delta t_{\mathrm{ref}})|.
\]

横轴分别为空间步长 \(h\) 和时间步长 \(\Delta t\)，纵轴为相对更细参考计算的终点绝对偏差，单位h或s；各用两条有标记的线表示问题3、4。移动区域若以 \(\Delta\xi\) 控制网格，则空间横轴写 \(\Delta\xi\)，不要把无量纲步长标为cm；也可统一以径向网格数 \(N_r\) 展示。

- 最细参考解不是解析真解，图题写“相对更细计算的偏差”，不要称“真实误差”。
- 若偏差为0，对数图不能直接画零；采用线性轴、非零差值或表格，不人为添加小数伪造误差。
- 同时固定终点定位容差等设置，避免所谓时间收敛实际反映输出采样的改变。
- 题面每1 s/60 s的结果采样间隔不等于内部积分步长；四位小数也不自动意味着相应数值精度。
- 可在图旁给小表记录终点与场变量最大差异；在条件允许时再检验守恒残差或可解析特例。
- 该图支持数值精度判断；不能命名为“实验验证”。目前附件没有提供药材内部温度或含水率实测序列，不能据此声称已完成内部场实验验证。

## 4. 篇幅取舍与备选图

优先保证图3、图5、图7能够被清楚阅读，这三处直接对应题目结果。图2承担模型解释，图8承担可靠性判断。附件原始表已经明确的数据无需再做饼图或装饰性统计图。

可选补图只在实际分析需要时增加：

1. **边界延拓/输入处理对终点的影响。** 比较几种有明确依据的边界延拓或输入平滑方案，画终点点图及变化范围。这对只有4 h环境观测、却需模拟数十小时的任务有实际价值。范围来自具体方案，不凭空画统计置信区间。
2. **同一物性下的固定半径/收缩对照。** 用于支持收缩作用归因；可先作为图7下图的第三条曲线，无需额外堆一张三维曲面。
3. **更完整剖面、末段放大或控制体检验。** 移到附录；只保留有新信息的部分。

精简到7个编号时，可将图6的半径曲线并入图7上方，坐标映射放入图2；若组合图文字变小，应恢复8个编号。不要为了压图号而牺牲可读性。

## 5. 全文风格与工具约定

| 元素 | 建议 |
|---|---|
| 基本版式 | 白底，浅灰辅助网格；图外图题，子图标(a)(b)；相同图类使用统一尺寸 |
| 曲线配色 | 环境温度橙色，水分相关主线蓝色，半径绿色，观测灰色，阈值红色虚线 |
| 比较规则 | 在同一幅比较图内，颜色优先区分模型/位置；不同量放入不同子图。时间剖面使用顺序色，不强行套物理量单色 |
| 热图 | 温度暖色连续色图，含水率蓝色连续色图；同量对照统一色标，各色条标单位 |
| 线型与标记 | 颜色外再用实/虚线和稀疏标记区分，保证灰度打印仍能辨认 |
| 文字 | 中文和数字/公式分别使用支持相应字符的清晰字体；按最终论文版面检查，通常图内约9—11 pt起步 |
| 尺寸 | 单幅组合图可按约15—16 cm正文宽度设计，再依据实际模板调整；2×2图优先占足宽度 |
| 输出 | 曲线与示意优先矢量PDF/SVG；含热图时可采用混合矢量与栅格导出；需PNG时按最终尺寸导出足够分辨率 |
| 数字 | 图、表、正文的终点和参数由同一份计算结果生成；显示保留位数不改变阈值计算 |
| 工具 | 若模型用Python，统一Matplotlib；若已用MATLAB，直接统一其绘图设置。物理示意使用程序矢量绘制或熟悉的矢量编辑器即可 |

往年参考关系：图2借鉴A016/A196的整体—局部示意；图3—4借鉴A242的统一多面板表达；图5、7借鉴2020炉温论文的阈值、交点和放大标注；图8借鉴A178/A217独立展示误差的习惯。具体颜色和排版是本题的设计选择，不是复刻原论文结论。

## 6. 实际出图顺序

\[
\text{图1、2、6（输入与模型）}
\longrightarrow
\text{求解并核对规定表格/结果文件}
\longrightarrow
\text{图3、4（短时结果）}
\longrightarrow
\text{图5、7（终点与全程）}
\longrightarrow
\text{图8（精度）}
\longrightarrow
\text{统一图题、单位与正文引用}.
\]

输入图和原理图现在即可制作；场图、终点图及收敛图须使用实际求解数据。后续根据实际结果删去重复子图，并填写图后结论，不预制“中心最后达标”“收缩一定加快干燥”等结论。

题面原件解压副本：[A题.pdf](E:/app/codex/课外/output/A题配图方案2026/source/A题/A题.pdf)。
历史风格依据：[18篇绘图学习总结](E:/app/codex/课外/output/paper_visual_study/往年A题论文绘图学习总结.md)。

